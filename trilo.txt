#include <iostream> #include <vector> #include <climits> using namespace std; int matrixChainOrder(const vector<int>& p, int n) { vector<vector<int>> dp(n, vector<int>(n, 0)); for (int len = 2; len < n; len++) { for (int i = 0; i < n - len; i++) { int j = i + len; dp[i][j] = INT_MAX; for (int k = i; k < j; k++) { dp[i][j] = min(dp[i][j], dp[i][k] + dp[k + 1][j] + p[i] * p[k + 1] * p[j]); } } } return dp[0][n - 1]; } int midThreeSubchainMultiply(const vector<int>& p, int n) { int mid = (n - 1) / 2; vector<int> firstChain(p.begin(), p.begin() + mid); int cost1 = matrixChainOrder(firstChain, mid); vector<int> secondChain(p.begin() + mid - 1, p.begin() + mid + 2); int cost2 = matrixChainOrder(secondChain, 3); vector<int> thirdChain(p.begin() + mid + 2, p.end()); int cost3 = matrixChainOrder(thirdChain, n - mid - 2); int mergeCost = p[mid - 1] * p[mid] * p[mid + 1]; return cost1 + cost2 + cost3 + mergeCost; } int main() { int n; cin >> n; vector<int> p(n); for (int i = 0; i < n; i++) { cin >> p[i]; } int result = midThreeSubchainMultiply(p, n); cout << result << endl; return 0; }


WORKED!